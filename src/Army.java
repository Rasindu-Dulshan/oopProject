import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class Army {
    // warrior is a character who is in the army
    List<Character> chosenWarriors = new ArrayList<Character>();

    public void createWarrirorList(Character character) {
        chosenWarriors.add(character);
    }
}
